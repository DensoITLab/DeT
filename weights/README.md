# Weights

Place local checkpoints in this directory, for example:

```text
weights/det.ckpt
```

Checkpoint files are ignored by Git. Publish release checkpoints through GitHub Releases, Hugging Face, or another artifact host, then link them from the top-level README.
